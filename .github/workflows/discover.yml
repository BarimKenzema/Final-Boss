name: Discover New Telegram Channels

on:
  schedule:
    # Runs every 16 minutes, which is about 4 times per hour.
    # This is an unusual schedule, which is good for avoiding patterns.
    - cron: '*/16 * * * *'
  workflow_dispatch:

jobs:
  discover:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Check out repository
        # This step is crucial to get the existing found_channels.txt file
        uses: actions/checkout@v4

      - name: Wait for a random time (1 to 3 minutes)
        run: sleep $((60 + RANDOM % 120))

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Telethon
        run: pip install telethon

      - name: Run Python Discovery Script
        env:
          API_ID: ${{ secrets.API_ID }}
          API_HASH: ${{ secrets.API_HASH }}
          TELEGRAM_SESSION_STRING: ${{ secrets.TELEGRAM_SESSION_STRING }}
        run: python discover_channels.py

      - name: Commit and push found channels
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "📜 Discovered new potential channels"
          # Only commit if the discovery file has changed
          file_pattern: found_channels.txt
